apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: consul
  labels:
    k8s-app: consul
spec:
  selector:
    matchLabels:
      app: consul
  namespaceSelector:
    matchNames:
    - vault
## disabling until we can figure out what valid endpoints should be
  endpoints:
  - port: exporter
    interval: 5s
